# This is Docker notes
Docker makes light weight container
It overcome problem of avialabilty 
we can say docker is container tool
- we can pull images from dockerhub
- we can create image using dockerfile
-then we build containers from images
 those container we can run like VM
 Docker commands
 - docker pull image name
 -docker --version
 -docker build -b image name